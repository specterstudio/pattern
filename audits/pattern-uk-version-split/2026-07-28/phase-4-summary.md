# Phase 4 Results

Phase 4 is complete in saved Webflow Designer state. The isolated V1/V2 asset
pilot is configured and verified, but nothing was published.

## Final immutable package

- Tag: `uk-version-split-v0.4.3`
- Commit: `c28741818d04b5207973368369e8287caad212e0`
- Package: `webflow/uk.pattern.com/version-split/`
- Generated files: 24
- CSS files: 18
- JavaScript files: 4
- Generated bytes: 137,438
- Structural validation: 50 of 50 checks passed
- Browser package validation: 18 CSS files, 468 CSSOM rules, 9 of 9
  behavior/runtime checks passed

The five externally referenced pilot assets return HTTP 200 with the expected
CSS or JavaScript content type:

- `css/shared.css`
- `css/v1.css`
- `css/v2.css`
- `css/features.css`
- `js/loader.js`

## Saved Webflow pilot state

### Pattern Intelligence — V2

- Page ID: `6a0e3cc3ba585cc12d1c2f43`
- Version marker: `data-pattern-version="v2"`
- Pilot marker: `data-pattern-asset-pilot="phase4"`
- Page head loads Shared, V2, and Features from `v0.4.3`
- The legacy Custom Code component instance was removed
- V1 CSS is not referenced

### Our Story — V1

- Page ID: `684987756493653cc7c5a546`
- Version marker: `data-pattern-version="v1"`
- Pilot marker: `data-pattern-asset-pilot="phase4"`
- Page head loads Shared, V1, and Features from `v0.4.3`
- The legacy Custom Code component instance was removed
- V2 CSS is not referenced

### Global loader

The existing site-footer freeform code is preserved. One external loader
reference was appended and advanced to `v0.4.3`.

The loader is inert unless a page contains both:

```html
data-pattern-asset-pilot="phase4"
data-pattern-version="v1" or data-pattern-version="v2"
```

Shared and Features CSS remain in the two pilot page heads during Phase 4.
They were not added to the global site head, so the other pages retain their
existing production asset path.

## Dependency corrections proven during verification

- Shared Header/Footer container tokens remain Shared.
- Unsuffixed `--grid-1` through `--grid-12` aliases remain Shared because the
  global Footer consumes them.
- Fluid unsuffixed type, button, and site-margin aliases remain Shared because
  global Header/Footer utilities consume them at responsive breakpoints.
- The optional `.page_main.cc-v1` production-fidelity block remains opt-in.
  A V1 version marker does not silently activate that combo class's visual
  overrides.
- V1 type and page-specific compatibility rules remain V1-scoped.
- V2 Content Wrapper alignment remains V2-scoped.

## Final browser verification

The immutable CDN package was simulated against the current published DOM while
removing the legacy bridge exactly as the saved pilot does.

- Viewports: 1440, 991, 767, and 479 pixels
- Pilot result sets: 8
- Pattern.com PI reference sets: 4
- Checks: 80
- Failures: 0

The matrix verifies:

- correct three CSS assets per pilot;
- correct V1 or V2 runtime selection;
- no stale opposite-version stylesheet;
- no asset request failures;
- no console errors;
- no horizontal overflow;
- V1 page width and height parity;
- V2 Content Wrapper alignment;
- shared Header/Footer container and height parity;
- Marketo form count, width, and height parity.

The full generated report is:

`output/playwright/pattern-uk-phase4/phase-4-live-simulation.json`

## Isolation control

The unpiloted Content Brief page (`69f9fe0ba11dd16bf945121e`) has no Phase 4
pilot marker and still contains one instance of the existing `Custom Code`
component (`89894b6d-cf3f-dfb6-51d8-63b0d19309e1`).

## Publication state

- Last updated: `2026-07-29T01:23:04.018Z`
- Last published: `2026-07-28T16:42:59.449Z`
- Full-site compiled: `2026-07-28T16:42:59.449Z`
- Published in Phase 4: no

## Rollback

Because Phase 4 is unpublished, production requires no rollback.

To revert the saved Designer pilot:

1. Remove the Phase 4 loader line from the site footer while preserving all
   pre-existing footer code.
2. Restore Pattern Intelligence head code to its pre-pilot Slick block only.
3. Clear the Our Story pilot head links.
4. Remove only `data-pattern-asset-pilot="phase4"` from both pilot roots; keep
   the Phase 2 `data-pattern-version` markers.
5. Reinsert the existing Custom Code component definition
   `89894b6d-cf3f-dfb6-51d8-63b0d19309e1` on both pages before `.page_wrap`.

The prior tags `v0.4.0`, `v0.4.1`, and `v0.4.2` remain immutable diagnostic
evidence, but they should not replace the verified `v0.4.3` package.
